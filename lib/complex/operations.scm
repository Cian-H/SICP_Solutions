(define (complex-add x y) (complex-apply-generic 'add x y))
(define (complex-sub x y) (complex-apply-generic 'sub x y))
(define (complex-mul x y) (complex-apply-generic 'mul x y))
(define (complex-div x y) (complex-apply-generic 'div x y))
