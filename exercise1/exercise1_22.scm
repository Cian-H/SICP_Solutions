(load "lib/math.scm")

(define (timed-prime-test n)
  (newline)
  (display n)
  (start-prime-test n (runtime)))

(define (start-prime-test n start-time)
  (if (prime? n)
    (report-prime (- (runtime) start-time))))

(define (report-prime elapsed-time)
  (display " *** ")
  (display elapsed-time)
  (newline))

(define (search-for-primes i n)
  (define (iter i n acc)
    (cond
      ((zero? n) acc)
      ((prime? i) (timed-prime-test i) (iter (+ i 2) (- n 1) (cons i acc)))
      (else (iter (+ i 2) n acc))))

  (iter i n '()))

(display "--- First 3 primes >1000 ---")
(newline)
(display (search-for-primes 1001 3))
(newline)
(newline)
(display "--- First 3 primes >10000 ---")
(newline)
(display (search-for-primes 10001 3))
(newline)
(newline)
(display "--- First 3 primes >100000 ---")
(newline)
(display (search-for-primes 100001 3))
(newline)
(newline)
(display "--- First 3 primes >1000000 ---")
(newline)
(display (search-for-primes 1000001 3))
(newline)
(newline)
